<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <% include partials/head %>
</head>
<body class="container">

<header>
    <% include partials/header %>
</header>

<main>
    <div class="container">
        <div class="row">
            <div class="col-md-4 col-xs-12">
                <p>Message to be sent:</p>
                <textarea readonly><%= message %></textarea>
            </div>
            <div class="col-md-8 col-xs-12">
                <% if(newstype===1) { %>
                <p>Message was be sent as a <strong>weekly</strong>news.</p>
                <% } %>
                <% if(newstype===2) { %>
                <p>Message was be sent as a <strong>daily</strong>news.</p>
                <% } %>
            </div>
        </div>
        <div class="row">
            <div class="col-md-12 col-xs-12">
                <p>Message was sent to <strong><%= numUsers %></strong> users.</p>
                <p>List of all users:</p>
                <ul>
                    <% users.forEach(function(user) { %>
                    <li><%= user.first_name %> <%= user.last_name %></li>
                    <% }) %>
                </ul>
            </div>
        </div>
    </div>
</main>

<footer>
    <% include partials/footer %>
</footer>

</body>
</html>
